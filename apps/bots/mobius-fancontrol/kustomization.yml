---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace:  bots

resources:
  - deployment.yml
  - mobius-configmap.yml
  - sealed-secret.yml

# insert a separate file as a configmap
configMapGenerator:
- name: mobius-envfile-configmap
  files:
  - .env