---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace:  bots

resources:
  - deployment.yml
  - tesseract-configmap.yml
  - sealed-secret.yml

# insert a separate file as a configmap
configMapGenerator:
- name: tesseract-envfile-configmap
  files:
  - .env