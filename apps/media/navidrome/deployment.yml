---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: navidrome
  namespace:  media
  labels:
    app: navidrome
spec:
  selector:
    matchLabels:
      app: navidrome
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: navidrome
    spec:
      restartPolicy: Always
      containers:
        - name: navidrome
          image: deluan/navidrome:latest@sha256:7c43af9f651654e97278be871705aae85a4ee0fa4b310337989699a8ada9b3ed
          resources:
            limits:
              cpu: 4
              memory: 4Gi
            requests:
              cpu: 1
              memory: 512Mi
          ports:
            - containerPort: 4533
          volumeMounts:
          - name: navidrome-music
            mountPath:  /music
          - name: navidrome-data
            mountPath:  /data
          - name: navidrome-config
            mountPath: /data/navidrome.toml
            subPath:  navidrome.toml
      volumes:
      - name: navidrome-music
        hostPath:
          path: /mnt/mobius/Music/library
      - name: navidrome-data
        persistentVolumeClaim:
          claimName:  navidrome-config-data
      - name: navidrome-config
        configMap:
          name: navidrome-configmap