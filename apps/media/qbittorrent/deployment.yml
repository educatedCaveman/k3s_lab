---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: qbittorrent
  namespace:  media
  labels:
    app: qbittorrent
spec:
  selector:
    matchLabels:
      app: qbittorrent
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: qbittorrent
    spec:
      restartPolicy: Always
      hostname: qbittorrent
      subdomain: internal
      initContainers:
      # use the default wireguard config
      - name: init-qbittorrent-qbt-config
        image: busybox@sha256:dc2d74b28e4cf8984fa52af1f39bc7c3d9c73760b41a74d629f5d11b1ab28616
        command: ["sh", "-c", "mkdir -p /config/config && mkdir -p /config/qBittorrent/config && cp /qbt/qBittorrent.conf /config/qBittorrent/config/qBittorrent.conf"]
        volumeMounts:
        - name: qbittorrent-config
          mountPath:  /config
        - name: qbittorrent-qbt-config
          mountPath:  /qbt
      # use the openvpn config
      - name: init-qbittorrent-ovpn-config
        image: busybox@sha256:dc2d74b28e4cf8984fa52af1f39bc7c3d9c73760b41a74d629f5d11b1ab28616
        command: ["sh", "-c", "mkdir -p /config/openvpn && cp /pia-ovpn/nl_amsterdam-aes-128-cbc-udp-dns.ovpn /config/openvpn/nl_amsterdam-aes-128-cbc-udp-dns.ovpn"]
        volumeMounts:
        - name: qbittorrent-config
          mountPath:  /config
        - name: pia-ovpn-config
          mountPath:  /pia-ovpn
      containers:
        - name: qbittorrent
          # using "latest" because the versions are something like "release-1f62614"
          image: binhex/arch-qbittorrentvpn:latest
          resources:
            limits:
              cpu: 16
              memory: 8Gi
            requests:
              cpu: 100m
              memory: 50Mi
          ports:
            - containerPort: 8080
          env:
          # these 3 might need to change
          - name: PUID
            value:  "1000"
          - name: PGID
            value:  "1000"
          - name: UMASK
            value:  "002"
          # - name: TZ
          #   value:  "America/New_York"
          - name: VPN_ENABLED
            value:  "yes"
          - name: VPN_PROV
            value:  "pia"
          - name: VPN_CLIENT
            value: "openvpn"
          - name: ENABLE_STARTUP_SCRIPTS
            value: "no"
          - name: ENABLE_PRIVOXY
            value: "yes"
          - name: STRICT_PORT_FORWARD
            value: "yes"
          - name: USERSPACE_WIREGUARD
            value: "no"
          - name: ENABLE_SOCKS
            value: "yes"
          - name: SOCKS_USER
            value: "admin"
          - name: SOCKS_PASS
            value: "socks"
          - name: LAN_NETWORK
            value:  "192.168.0.0/16"
          - name: VPN_USER
            valueFrom:
              secretKeyRef:
                name: qbittorrent-pia-vpn-creds
                key: username
          - name: VPN_PASS
            valueFrom:
              secretKeyRef:
                name: qbittorrent-pia-vpn-creds
                key: password
          securityContext:
            capabilities: # Needed for VPN
              add:
                - NET_ADMIN
          volumeMounts:
          - name: qbittorrent-config
            mountPath:  /config
          - name: qbittorrent-data
            mountPath:  /data
      volumes:
        - name: qbittorrent-config
          persistentVolumeClaim:
            claimName:  qbittorrent-config-pvc
        - name: qbittorrent-data
          persistentVolumeClaim:
            claimName:  qbittorrent-nfs-data-pvc
        - name: qbittorrent-qbt-config
          configMap:
            name: qbittorrent-qbt-configmap
        - name: pia-ovpn-config
          configMap:
            name: pia-ovpn-configmap