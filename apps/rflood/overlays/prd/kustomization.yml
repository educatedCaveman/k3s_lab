---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: prd

# # insert a separate file as a configmap
# configMapGenerator:
# - name: CHANGE_ME-configmap
#   files:
#   - config.yml

# # needed for pruning in the Jenkinsfile
# generatorOptions:
#   labels:
#     app: CHANGE_ME

resources:
- ../../base

namePrefix: prd-

patches:
# - path: volumes.yml
- path: svc_IP.yml
