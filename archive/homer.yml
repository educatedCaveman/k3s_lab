---
apiVersion: v1
kind: Service
metadata:
  name: homer
spec:
  ipFamilyPolicy: SingleStack
  selector:
    app: homer
  ports:
    - port: 8080
      targetPort: 8080
  type: LoadBalancer
  loadBalancerIP: 192.168.13.20

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: homer
  labels:
    app: homer
  # namespace: default
spec:
  selector:
    matchLabels:
      app: homer
  strategy:
    type: Recreate
  replicas: 1
  template:
    metadata:
      labels:
        app: homer
    spec:
      os: { name: linux }
      nodeSelector:
        kubernetes.io/os: linux
      restartPolicy: Always
      containers:
        - name: homer
          # image: b4bz/homer:latest@sha256:c1f5bfcfd2fe2ea07b8c5a574d9deb5aa5054f2f3582a1a3f8833394d2a06656
          ports:
            - containerPort: 8080
          volumeMounts:
          - name: homer-nfs
            mountPath: /www/assets
      volumes:
        - name: homer-nfs
          hostPath:
            path: /mnt/k3s-data/dev-test/apps/homer/assets/
            type: Directory
